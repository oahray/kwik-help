export const PERMITTED_AUTH_PARAMS = Object.freeze([
  'username',
  'email',
  'password'
]);

export const PERMITTED_TICKET_PARAMS = Object.freeze([
  'title',
  'description'
]);

export const PERMITTED_COMMENT_PARAMS = Object.freeze([
  'body'
]);
